[pytest]
# `tests/` = the classic app + ML core. `studio/tests/` = the Studio app's own
# suite. When working on Studio, run just `pytest studio/tests`.
testpaths = tests studio/tests
pythonpath = .
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
